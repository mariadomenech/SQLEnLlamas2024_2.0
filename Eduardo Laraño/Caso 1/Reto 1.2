SELECT
  C.CUSTOMER_ID CLIENTE,
  COUNT(DISTINCT S.ORDER_DATE) NUM_VISITAS
FROM  CASE01.SALES S
RIGHT JOIN CASE01.CUSTOMERS C
  ON S.CUSTOMER_ID = C.CUSTOMER_ID
GROUP BY C.CUSTOMER_ID
ORDER BY C.CUSTOMER_ID;
